// Example config in a TS file: placeholder tokens and a sample bearer header.
// None are live credentials — a scanner must not raise these.
export const config = {
  apiKey: "YOUR_API_KEY_HERE",
  authHeader: "Bearer example-token-do-not-use",
  clientSecret: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
};
